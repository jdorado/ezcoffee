# Copy this outside the repository on the production host and set mode 0600.
# Pass it to Docker Compose with --env-file. Never commit the real version.
MONGO_URL=mongodb+srv://<username>:<password>@<cluster-host>/
MONGO_DB=ezcoffee
COFFEE_WEB_PORT=18085
